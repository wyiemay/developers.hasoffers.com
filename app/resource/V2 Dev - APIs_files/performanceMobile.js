/*
 * Mobile specific performance report controller
 */

(function(root, $, undefined) {
  'use strict';

  root.Application.controller('Controllers.performanceMobile', ['$scope', function($scope) {
    $scope.message = 'Mobile performance report improvements are in progress.';
  }]);
})(this, jQuery);
